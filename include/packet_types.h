#ifndef _PACKET_TYPES_H
#define _PACKET_TYPES_H

#include <Arduino.h>

// alternative version, not done yet
/*
typedef struct
{
    uint8_t device_mac[6];
    uint8_t header[2];
    uint8_t crc;
    uint8_t data[128];
    uint16_t data_length;

} client_packet;
*/


#endif
